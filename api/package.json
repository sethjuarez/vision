{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "build:production": "npm run prestart && npm prune --production",
    "watch": "tsc --w",
    "prestart": "npm run build && func extensions install",
    "start:host": "func start",
    "start": "npm-run-all --parallel start:host watch",
    "test": "echo \"No tests yet...\""
  },
  "devDependencies": {
    "@types/node": "^14.0.14",
    "@azure/functions": "^1.0.1-beta1",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.3.3"
  },
  "dependencies": {
    "@azure/cognitiveservices-customvision-training": "^5.1.1",
    "@azure/cosmos": "^3.7.4",
    "axios": "^0.19.2",
    "luxon": "^1.24.1",
    "moment-timezone": "^0.5.31",
    "striptags": "^3.1.1",
    "uuid": "^8.3.0"
  }
}
